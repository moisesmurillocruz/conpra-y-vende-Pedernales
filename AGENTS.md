## Cursor Cloud specific instructions

This repository currently contains only a GPLv3 `LICENSE` file with no application code, dependencies, build system, or configuration files. There is nothing to build, lint, test, or run at this time.

When application code is added, update this section with:
- How to install dependencies and which package manager to use.
- How to run the development server.
- How to run linting and tests.
- Any non-obvious setup caveats or environment requirements.
